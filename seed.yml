before_compile:
  - echo "Before compile"

before_build:
  - echo "Before build"
  - export LOCALSTACK_API_KEY=4d5e8d5a
  - echo "UP  Localstack container"
  - docker-compose up -d localstack
  - sleep 5s
  - echo "Running all tests"
  - docker-compose up --exit-code-from condo-convid-19-tests condo-convid-19-tests
before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"
